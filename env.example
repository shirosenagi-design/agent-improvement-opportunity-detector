# PROJECT 04 — local environment template
# Never commit a real secret. Use either variable below.
PROJECT04_OPENAI_API_KEY=
OPENAI_API_KEY=

# Default model used by the current prototype. Override if needed.
OPENAI_MODEL_ID=gpt-5.6-sol
