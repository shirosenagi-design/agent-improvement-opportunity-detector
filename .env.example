MODEL_PROVIDER=bedrock
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=global.anthropic.claude-sonnet-4-6
# Use the standard AWS credential chain. Never put credentials in source control.

# For OpenAI, set MODEL_PROVIDER=openai and provide OPENAI_MODEL_ID here.
# Set OPENAI_API_KEY only in the process environment; do not store its value in this file.
OPENAI_MODEL_ID=
